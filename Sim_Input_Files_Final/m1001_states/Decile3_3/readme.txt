Need to unzip data files inside current directory
Ensure only unzipped data files remain in folder before running as code automatically reads in all files in this folder. If extra (eg zipped files) remain, code will crash. 
